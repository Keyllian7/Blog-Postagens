<h1>Lista de Categorias:</h1>
<hr>
<a href="/admin/categorias/adicionar"><button class="btn btn-success">Adicionar Categoria</button></a>

{{#each categorias}}
    <div class="card mt-4">
        <div class="card-body">
            <h4>{{nome}}</h4>
            <small>Slug: {{slug}}</small>
            <br>
            <small>Data de criação: {{date}}</small>
            <br>
            <a href="/admin/categorias/editar/{{_id}}"><button class="btn btn-success mt-2">Editar Categoria</button></a>
            <form action="/admin/categorias/deletar" method="POST">
                <input type="hidden" name="id" value="{{_id}}">
                <button type="submit" class="btn btn-danger mt-2">Deletar Categoria</button>
            </form>
        </div>
    </div>
{{else}}
    <h4 class="mt-4">Nenhuma Categoria Registrada!</h4>
{{/each}}